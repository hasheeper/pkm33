# 宝可梦战斗引擎 - 完整文档

> **版本**: v2.0  
> **最后更新**: 2026-01-22  
> **文档类型**: 功能说明 + 用户指南 + 开发文档

---

## 📋 目录

1. [项目概述](#项目概述)
2. [核心功能](#核心功能)
3. [系统架构](#系统架构)
4. [战斗机制详解](#战斗机制详解)
5. [特色系统](#特色系统)
6. [用户指南](#用户指南)
7. [开发文档](#开发文档)
8. [数据结构](#数据结构)
9. [API 参考](#api-参考)

---

## 项目概述

### 简介

这是一个基于 Web 技术的**宝可梦对战模拟引擎**，采用现代化的 ES Module 架构，实现了完整的宝可梦对战系统，包括：

- **完整的战斗机制**：伤害计算、属性克制、能力变化、状态异常
- **世代机制支持**：Mega 进化、Z 招式、极巨化、太晶化
- **创新系统**：对冲系统、古武风格、训练家指挥、杀意感知
- **智能 AI**：四档难度（Easy/Normal/Hard/Expert），支持战术换人和预判
- **动漫风格成长**：基于战斗表现的等级建议系统

### 技术栈

- **前端框架**: 原生 JavaScript (ES6+)
- **模块系统**: ES Modules
- **数据来源**: Pokémon Showdown (数据移植)
- **本地化**: PSChina 社区翻译
- **UI 风格**: 剑盾 + 女神异闻录 5 混合风格

---

## 核心功能

### 1. 战斗系统

#### 1.1 伤害计算引擎 (`battle/battle-calc.js`)

**核心功能**：
- **精确伤害计算**：基于 Pokémon Showdown 的伤害公式
- **属性克制系统**：18 种属性的完整克制关系
- **能力等级修正**：±6 级能力变化 (Boosts)
- **暴击系统**：暴击率计算、急所等级
- **命中判定**：技能命中率、回避率修正
- **多段攻击**：连续攻击次数随机化

**特性修正**：
- 皮肤系特性（妖精皮肤、飞行皮肤等）：属性转换 + 1.2 倍威力
- 适应力：本系招式 2.0 倍（替代默认 1.5 倍）
- 铁拳：拳类招式 1.2 倍
- 硬爪：接触类招式 1.3 倍
- 技术高手：低威力招式 1.5 倍
- 破格系特性（破格、涡轮火焰、兆级电压）：无视对方特性

**道具修正**：
- 讲究系道具（讲究头巾/眼镜/围巾）：1.5 倍威力
- 生命宝珠：1.3 倍威力，反伤 10%
- 专爱系道具：锁定招式
- 弱点保险：受到效果绝佳攻击时特攻/攻击 +2

#### 1.2 招式效果系统 (`battle/battle-effects.js`)

**副作用处理**：
- **能力变化**：攻击/防御/特攻/特防/速度/命中/回避 ±1~3 级
- **反伤技能**：自爆、舍身冲撞、伏特攻击等
- **吸血技能**：吸取拳、终极吸取、木角等
- **状态异常**：麻痹、中毒、灼伤、睡眠、冰冻
- **接触反馈**：静电、火焰之躯、粗糙皮肤等

**特殊招式**：
- **Pivot 招式**：急速折返、伏特替换、蜻蜓返等（攻击后换人）
- **强制换人**：吼叫、龙尾、抛下狠话等
- **自我强化**：剑舞、龙之舞、冥想等
- **场地设置**：隐形岩、撒菱、光墙、反射壁等

#### 1.3 回合执行系统 (`battle/battle-turns.js`)

**回合流程**：
1. **回合开始**：递减风格冷却、场地效果计时
2. **优先级判定**：技能优先级 → 速度比较 → 同速随机
3. **行动执行**：
   - 状态阻断检测（麻痹、睡眠、冰冻、畏缩）
   - 特性钩子触发（懒惰、变幻自如等）
   - 招式执行
   - 副作用处理
4. **回合结束**：
   - 天气伤害（沙暴、冰雹）
   - 中毒/灼伤伤害
   - 剧毒累积
   - 场地效果递减

#### 1.4 换人系统 (`battle/battle-switch.js`)

**换人类型**：
- **主动换人**：玩家选择换人
- **Pivot 换人**：急速折返等技能触发
- **强制换人**：吼叫、龙尾等技能强制
- **倒下换人**：宝可梦 HP 归零后强制换人

**入场特性触发**：
- 威吓：降低对方攻击 1 级
- 降雪/降雨/沙暴/日照：改变天气
- 电气场地/精神场地等：设置场地
- 画皮/冰砌鹅：一次性护盾

---

### 2. 世代机制

#### 2.1 Mega 进化系统 (`mechanics/mega-evolution.js`)

**支持形态**：
- **单 Mega**：路卡利欧、暴蝾螈、巨金怪等
- **双 Mega**：喷火龙 X/Y、超梦 X/Y
- **原始回归**：固拉多、盖欧卡（战斗开始自动触发）
- **究极爆发**：奈克洛兹玛（Ultra Burst）
- **加冕形态**：苍响、藏玛然特（战斗开始自动触发）

**机制特点**：
- 每场战斗限用一次
- 携带对应 Mega 石
- 回合开始前触发
- 种族值、属性、特性变化

#### 2.2 Z 招式系统 (`mechanics/z-moves.js`)

**Z 招式类型**：
- **通用 Z 招式**：18 种属性对应的通用 Z 招式
  - 例：火 Z → 超强极限爆焰弹
- **专属 Z 招式**：特定宝可梦 + 特定招式
  - 例：皮卡丘 + 十万伏特 → 千万伏特
  - 智皮专属：帽子皮卡丘 + 雷电 → 千万伏特（优先级最高）

**威力计算**：
- 基于原招式威力映射到 Z 招式威力
- 变化技转换为 Z 强化效果

**专属 Z 矩阵**：
- 皮卡丘系：千万伏特、皮卡皮卡必杀击、阿罗雷冲浪手
- 御三家：炽焰咆哮虎、西狮海壬、狙射树枭专属 Z
- 传说宝可梦：究极奈克洛兹玛、索尔迦雷欧、露奈雅拉等

#### 2.3 极巨化系统 (`mechanics/dynamax.js`)

**极巨化类型**：
- **通用极巨化**：所有宝可梦可用，招式转换为 Max 招式
- **超极巨化**：特定宝可梦专属，拥有 G-Max 招式

**G-Max 专属招式**（部分）：
- 喷火龙：G-Max 地狱灭焰
- 妙蛙花：G-Max 灰飞烟灭
- 水箭龟：G-Max 炮击
- 皮卡丘：G-Max 万雷轰顶
- 伊布：G-Max 抱抱
- 卡比兽：G-Max 满腹大作战

**机制特点**：
- HP 翻倍（视觉效果）
- 持续 3 回合
- 所有招式转换为极巨招式
- 极巨招式附带场地/天气效果

#### 2.4 太晶化系统

**太晶化机制**：
- 改变宝可梦属性为太晶属性
- 本系招式享受 2.0 倍加成（替代 1.5 倍）
- 每场战斗限用一次
- 特殊招式：Tera Blast（根据攻击/特攻自动选择物理/特殊）

---

### 3. 特色创新系统

#### 3.1 对冲系统 (Clash System) (`mechanics/clash-system.js`)

**核心概念**：
当双方同时使用攻击技能时，触发"对冲判定"，模拟招式碰撞。

**Clash Type 分类**：
- **SOLID（实体）**：近身攻击、岩石投射、金属投射
- **BEAM（光束）**：能量光束、投射能量
- **WAVE（波动）**：场地技能、范围攻击、天降技能
- **PIERCE（穿刺）**：切割、穿刺类招式

**对冲矩阵**：
```
BEAM vs BEAM   → CP Check（威力对拼）
BEAM vs SOLID  → BEAM 优势 +0.5
BEAM vs WAVE   → BEAM 穿透 +1.0
BEAM vs PIERCE → PIERCE 切开 -0.5

SOLID vs SOLID → CP Check + 暴击加成
SOLID vs WAVE  → SOLID 突破 +0.8
SOLID vs PIERCE → 格挡对决 + 暴击加成

WAVE vs WAVE   → CP Check
WAVE vs PIERCE → PIERCE 穿过 -0.5

PIERCE vs PIERCE → 交叉斩击 + 高暴击加成
```

**Clash Power 计算**：
```javascript
CP = 招式威力 × 属性克制 × 能力修正 × 特性修正 × 道具修正
```

**对冲结果**：
- **Overpower（碾压）**：CP 差距 > 2.0，完全压制
- **Dominate（压制）**：CP 差距 1.5~2.0，占据上风
- **Edge（微弱优势）**：CP 差距 1.2~1.5，略胜一筹
- **Stalemate（僵持）**：CP 差距 < 1.2，双方受伤

#### 3.2 杀意感知系统 (Insight System)

**触发条件**：
- 玩家速度慢于敌方（后手）
- 敌方使用攻击技能

**Insight Level**：
- **Level 1（微弱杀意）**：伤害 < 25% HP
- **Level 2（明显杀意）**：伤害 25%~50% HP
- **Level 3（强烈杀意）**：伤害 50%~75% HP
- **Level 4（致命杀意）**：伤害 > 75% HP

**效果**：
- 显示预警 UI
- 提供对冲加成（后手反击）
- 战术指挥菜单触发

#### 3.3 古武系统 (Move Styles) (`mechanics/move-styles.js`)

**三种风格**：
- **道（Normal）**：默认状态，无修正
- **迅（Agile）**：优先度 +1，威力 ×0.75
- **刚（Strong）**：威力 ×1.5，优先度 -1

**冷却机制**：
- 使用迅/刚风格后进入 2 回合冷却
- 冷却期间只能使用道风格
- UI 显示"休"字标记

**战术应用**：
- 迅风格：抢先手、打断对方
- 刚风格：一击必杀、破盾

#### 3.4 训练家战术指挥系统

**四种指令**：
- **DODGE（回避）**：触发条件：Insight Level ≥ 3
  - 效果：回避率大幅提升
- **FOCUS（集中）**：触发条件：HP < 30%
  - 效果：暴击率 +2 级
- **ENDURE（坚守）**：触发条件：HP < 20%
  - 效果：必定承受致命一击（HP 保留 1）
- **LISTEN（倾听）**：触发条件：羁绊值 ≥ 80
  - 效果：特殊能力解锁

**触发机制**：
- 满足条件时在技能菜单前显示指令轮盘
- 每回合限用一次
- 消耗对应资源（Insight/羁绊值等）

---

### 4. AI 系统 (`engine/ai-engine.js`)

#### 4.1 四档难度

**Easy（简单）**：
- 80% 随机选择
- 20% 选择最优招式
- 不会换人

**Normal（普通）**：
- 60% 最优招式
- 30% 次优招式
- 10% 随机
- 偶尔换人

**Hard（困难）**：
- 总是选择当前最优招式
- 考虑属性克制、威力、命中率
- 支持风格选择（50% 概率）

**Expert（专家）**：
- 大局观 AI
- 战略性换人（清除负面状态、属性优势）
- 折返技能战术评分
- 读换预判（预测玩家换人）
- 斩杀计算（优先击杀残血目标）
- 风险评估（脆皮输出手更惜命）

#### 4.2 AI 评分系统

**招式评分因子**：
- **基础伤害**：预计伤害值
- **属性克制**：效果绝佳 +200，效果不佳 -100
- **命中率**：低命中率扣分
- **副作用**：能力提升/降低、状态异常
- **特殊战术**：
  - 折返技能（U-turn）：+150~300（根据换人价值）
  - 强化技能（剑舞）：+100~200（根据局势）
  - 治疗技能（自我再生）：+50~150（根据 HP）

**换人评分因子**：
- 属性优势：+300
- 清除负面状态：+200
- 避免被击杀：+500
- 保护核心输出：+400

---

### 5. 成长系统 (`systems/growth-system.js`)

#### 5.1 动漫风格等级建议

**设计理念**：
不计算具体经验值，而是基于战斗表现给出"突破等级"建议。

**成长因子**：

1. **结果基础分**：
   - 胜利：+0.3
   - 失败：+0.2（教训也是成长）
   - 捕获：+0.2
   - 逃跑：0

2. **对手强度**：
   - 四天王/冠军：+3
   - Boss（Expert/Hard）：+2
   - 训练家：+1
   - 野生：×0.6（削弱）

3. **等级跃迁（下克上）**：
   - 等级差 ≥30：+7（神话级）
   - 等级差 ≥20：+5（神迹级）
   - 等级差 ≥10：+3
   - 等级差 ≥5：+1.5
   - 等级差 -5~-10：-0.6（虐菜惩罚）
   - 等级差 < -20：-2（极端虐菜）

4. **戏剧性因子**：
   - 残血反杀（HP < 10%）：+2
   - 残血险胜（HP < 20%）：+1.5
   - 无伤碾压（Rank S）：
     - 对手等级高：+2（技巧取胜）
     - 对手等级低：-1（炸鱼惩罚）

5. **机制掌握**：
   - 使用 Mega/Z/Max/Tera：各 +0.5
   - 羁绊进化：+1.5
   - 对冲次数：+0.3/次（上限 1）

**成长评级**：
- ≥10 级：超越神话的壮举
- ≥8 级：灵魂深处的觉醒
- ≥5 级：基因序列的升华
- ≥3 级：理解战斗真谛
- 2 级：显著成长
- 1 级：宝贵经验
- 0 级：无锻炼效果

---

### 6. 捕获系统 (`systems/catch-system.js`)

#### 6.1 捕获率计算

**公式**：
```javascript
baseCatchRate = 350 - (种族值总和 / 1.8)
hpFactor = (3 × maxHP - 2 × currHP) / (3 × maxHP)
finalChance = (baseCatchRate × hpFactor × ballMultiplier × statusFactor) / 255
```

**精灵球倍率**：
- 普通球：1.0
- 超级球：1.5
- 高级球：2.0
- 大师球：255（必定捕获）

**捕获动画**：
- 摇晃次数基于捕获率
- 捕获率 > 0.7：摇晃 3 次后失败
- 捕获率 0.4~0.7：摇晃 2 次后失败
- 捕获率 < 0.4：摇晃 1 次后失败
- 捕获成功：摇晃 3 次后"咚！"

---

## 系统架构

### 模块结构

```
pkm12/
├── data/                    # 数据层
│   ├── pokedex-data.js      # 宝可梦种族值数据
│   ├── moves-data.js        # 招式数据
│   ├── trainer-data.js      # 训练家数据
│   └── move-constants.js    # 招式常量
│
├── engine/                  # 核心引擎
│   ├── battle-engine.js     # 战斗引擎核心
│   ├── ability-handlers.js  # 特性处理器
│   ├── move-handlers.js     # 招式处理器
│   ├── move-effects.js      # 招式效果
│   ├── items-data.js        # 道具数据
│   └── ai-engine.js         # AI 引擎
│
├── battle/                  # 战斗系统
│   ├── battle-calc.js       # 伤害计算
│   ├── battle-damage.js     # 伤害应用
│   ├── battle-effects.js    # 副作用处理
│   ├── battle-turns.js      # 回合执行
│   └── battle-switch.js     # 换人系统
│
├── mechanics/               # 世代机制
│   ├── mega-evolution.js    # Mega 进化
│   ├── z-moves.js           # Z 招式
│   ├── dynamax.js           # 极巨化
│   ├── clash-system.js      # 对冲系统
│   ├── move-styles.js       # 古武系统
│   └── form-change/         # 形态变化
│
├── systems/                 # 辅助系统
│   ├── catch-system.js      # 捕获系统
│   ├── growth-system.js     # 成长系统
│   ├── audio-manager.js     # 音频管理
│   ├── bgm-system.js        # BGM 系统
│   └── translations.js      # 翻译系统
│
├── ui/                      # UI 层
│   ├── ui-menus.js          # 菜单系统
│   ├── ui-renderer.js       # 渲染器
│   ├── ui-sprites.js        # 精灵图管理
│   └── ui-trainer-hud.js    # 训练家 HUD
│
└── main.js                  # ES Module 入口
```

### 数据流

```
用户输入 → UI 层 → 战斗系统 → 引擎层 → 数据层
                    ↓
                AI 决策
                    ↓
                回合执行
                    ↓
                视觉更新
```

---

## 战斗机制详解

### 伤害计算公式

```javascript
// 基础伤害
baseDamage = ((2 × level / 5 + 2) × power × A / D) / 50 + 2

// 修正系数
damage = baseDamage × 
         targets × 
         weather × 
         critical × 
         random × 
         STAB × 
         typeEffectiveness × 
         burn × 
         other
```

**各项说明**：
- `level`：攻击方等级
- `power`：招式威力
- `A`：攻击方攻击/特攻（含能力等级修正）
- `D`：防御方防御/特防（含能力等级修正）
- `targets`：多目标修正（0.75）
- `weather`：天气修正（晴天火系 1.5，雨天水系 1.5）
- `critical`：暴击修正（1.5 或 2.0）
- `random`：随机因子（0.85~1.0）
- `STAB`：本系加成（1.5 或 2.0 太晶化）
- `typeEffectiveness`：属性克制（0, 0.25, 0.5, 1, 2, 4）
- `burn`：灼伤修正（物理招式 0.5）
- `other`：其他修正（特性、道具等）

### 能力等级修正

```javascript
// 能力等级范围：-6 ~ +6
const boostMultipliers = {
    '-6': 2/8, '-5': 2/7, '-4': 2/6, '-3': 2/5, '-2': 2/4, '-1': 2/3,
    '0': 1,
    '+1': 3/2, '+2': 4/2, '+3': 5/2, '+4': 6/2, '+5': 7/2, '+6': 8/2
}
```

### 属性克制表

```javascript
TYPE_CHART = {
    'Normal':   { 'Rock': 0.5, 'Ghost': 0, 'Steel': 0.5 },
    'Fire':     { 'Fire': 0.5, 'Water': 0.5, 'Grass': 2, 'Ice': 2, ... },
    'Water':    { 'Fire': 2, 'Water': 0.5, 'Grass': 0.5, ... },
    // ... 18 种属性完整克制关系
}
```

---

## 用户指南

### 基础操作

#### 战斗界面

```
┌─────────────────────────────────────┐
│  敌方宝可梦                          │
│  ┌─────────────────┐                │
│  │ 名称  Lv.50     │                │
│  │ HP: ████████░░  │                │
│  └─────────────────┘                │
│                                     │
│           [敌方精灵图]              │
│                                     │
│                                     │
│           [我方精灵图]              │
│                                     │
│  ┌─────────────────┐                │
│  │ 名称  Lv.80     │                │
│  │ HP: 245/280     │                │
│  └─────────────────┘                │
│                                     │
│  [战斗] [背包] [宝可梦] [逃跑]      │
└─────────────────────────────────────┘
```

#### 技能选择

```
┌─────────────────────────────────────┐
│  选择招式：                          │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ 火焰放射 │  │ 龙之波动 │        │
│  │ 火 特殊  │  │ 龙 特殊  │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ 地震     │  │ 守住     │        │
│  │ 地 物理  │  │ 一 变化  │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  [太极球: 道] [Mega] [Z] [Max]     │
└─────────────────────────────────────┘
```

### 进阶技巧

#### 1. 使用古武风格

**迅风格（抢先手）**：
1. 点击太极球切换到"迅"
2. 选择招式
3. 优先度 +1，威力 ×0.75
4. 进入 2 回合冷却

**刚风格（一击必杀）**：
1. 点击太极球切换到"刚"
2. 选择招式
3. 威力 ×1.5，优先度 -1
4. 进入 2 回合冷却

**战术应用**：
- 迅风格打断对方强化（剑舞、龙舞）
- 刚风格破盾（画皮、冰砌鹅）
- 刚风格斩杀（确保击杀）

#### 2. 触发对冲

**条件**：
- 双方同时使用攻击技能
- 玩家后手且触发 Insight

**操作**：
1. 观察 Insight 预警（杀意等级）
2. 选择高威力招式
3. 对冲判定自动触发
4. 根据 Clash Type 获得优势/劣势

**最佳实践**：
- BEAM 招式克制 WAVE 招式
- SOLID 招式克制 WAVE 招式
- PIERCE 招式克制 BEAM 招式
- 同类型对冲看威力（CP Check）

#### 3. 训练家指令

**DODGE（回避）**：
- 触发条件：Insight Level ≥ 3（强烈杀意）
- 效果：大幅提升回避率
- 适用场景：对方使用一击必杀技能

**FOCUS（集中）**：
- 触发条件：HP < 30%
- 效果：暴击率 +2 级
- 适用场景：残血反杀

**ENDURE（坚守）**：
- 触发条件：HP < 20%
- 效果：必定承受致命一击（HP 保留 1）
- 适用场景：绝境翻盘

#### 4. Mega 进化时机

**最佳时机**：
- 对方宝可梦属性被克制
- 需要速度优势抢先手
- 需要特性变化（如 Mega 暴蝾螂的飞行皮肤）

**注意事项**：
- 每场战斗限用一次
- 必须携带对应 Mega 石
- Mega 进化后无法退化

#### 5. Z 招式使用

**选择时机**：
- 斩杀残血目标
- 破盾（画皮、冰砌鹅）
- 突破防御强化（铁壁、宇宙力量）

**专属 Z 优先级**：
- 智皮（千万伏特）> 普通皮卡丘 Z
- 检查是否满足专属 Z 条件

---

## 开发文档

### 添加新招式

#### 1. 在 `data/moves-data.js` 添加数据

```javascript
export const MOVES = {
    // ... 现有招式
    
    'newmove': {
        num: 9999,
        name: 'New Move',
        cnName: '新招式',
        type: 'Fire',
        category: 'Special',
        basePower: 90,
        accuracy: 100,
        pp: 15,
        priority: 0,
        flags: { protect: 1, mirror: 1 },
        secondary: {
            chance: 10,
            status: 'brn'  // 10% 概率灼伤
        },
        desc: '新招式的描述'
    }
}
```

#### 2. 添加特殊效果（可选）

在 `engine/move-handlers.js` 添加处理器：

```javascript
export const MoveHandlers = {
    // ... 现有处理器
    
    'New Move': {
        // 招式使用前检查
        onUse(user, target, logs, battle, isPlayer) {
            // 自定义逻辑
            return { failed: false };
        },
        
        // 命中后效果
        onHit(user, target, damage, logs, battle) {
            // 自定义效果
            logs.push('特殊效果触发！');
        },
        
        // 修改招式属性
        onModifyType(move, user, battle) {
            // 动态修改属性
            return 'Electric';
        }
    }
}
```

### 添加新特性

#### 1. 在 `engine/ability-handlers.js` 添加处理器

```javascript
export const AbilityHandlers = {
    // ... 现有特性
    
    'New Ability': {
        // 修改伤害
        onModifyDamage(baseDamage, attacker, defender, move) {
            if (move.type === 'Fire') {
                return baseDamage * 1.5;  // 火系招式 1.5 倍
            }
            return baseDamage;
        },
        
        // 修改招式属性
        onModifyType(move, user) {
            if (move.type === 'Normal') {
                return 'Fairy';  // 普通系转妖精系
            }
        },
        
        // 入场效果
        onSwitchIn(pokemon, battle) {
            log(`${pokemon.cnName} 的新特性发动了！`);
        },
        
        // 受到攻击时
        onDamagingHit(damage, attacker, defender, move) {
            if (move.flags?.contact) {
                // 接触类招式反伤
                const recoil = Math.floor(attacker.maxHp / 8);
                attacker.currHp = Math.max(0, attacker.currHp - recoil);
                log(`${attacker.cnName} 受到了接触伤害！`);
            }
        }
    }
}
```

### 添加新道具

#### 1. 在 `engine/items-data.js` 添加数据

```javascript
export const ITEMS = {
    // ... 现有道具
    
    'newitem': {
        name: 'New Item',
        cnName: '新道具',
        desc: '新道具的描述',
        spritenum: 999
    }
}
```

#### 2. 添加道具效果

```javascript
export const ItemEffects = {
    // ... 现有效果
    
    'New Item': {
        // 修改伤害
        onModifyDamage(baseDamage, holder, move) {
            if (move.type === 'Water') {
                return baseDamage * 1.2;
            }
            return baseDamage;
        },
        
        // 回合结束效果
        onResidual(holder, battle) {
            // 每回合恢复 HP
            const heal = Math.floor(holder.maxHp / 16);
            holder.currHp = Math.min(holder.maxHp, holder.currHp + heal);
            log(`${holder.cnName} 恢复了 HP！`);
        }
    }
}
```

### 自定义 AI 难度

#### 1. 在 `engine/ai-engine.js` 添加新难度

```javascript
export function getCustomAiAction(aiPoke, playerPoke, aiParty, battleContext) {
    // 自定义 AI 逻辑
    const rankedMoves = rankMovesByScore(aiPoke, playerPoke, aiParty);
    
    // 自定义评分调整
    rankedMoves.forEach(m => {
        if (m.move.type === 'Dragon') {
            m.score += 100;  // 偏好龙系招式
        }
    });
    
    // 返回最优招式
    return {
        type: 'move',
        move: rankedMoves[0].move
    };
}
```

### 扩展对冲系统

#### 1. 添加新的 Clash Type

在 `mechanics/clash-system.js` 修改：

```javascript
const CLASH_TYPE = {
    SOLID: 'SOLID',
    BEAM: 'BEAM',
    WAVE: 'WAVE',
    PIERCE: 'PIERCE',
    MYSTIC: 'MYSTIC'  // 新类型：神秘
};

// 添加到对冲矩阵
const CLASH_MATRIX = {
    // ... 现有矩阵
    
    'MYSTIC': {
        'BEAM':   { interaction: 'absorb', advantage: 0.3, critBonus: 0 },
        'SOLID':  { interaction: 'phase', advantage: 0.5, critBonus: 0 },
        'WAVE':   { interaction: 'resonate', advantage: 0.8, critBonus: 0 },
        'PIERCE': { interaction: 'deflect', advantage: -0.2, critBonus: 0 },
        'MYSTIC': { interaction: 'cpCheck', advantage: 0, critBonus: 0.4 }
    }
};
```

#### 2. 添加招式到新类型

```javascript
const MYSTIC_OVERRIDE = [
    'psychic', 'psyshock', 'futuresight', 'storedpower',
    'expandingforce', 'psychicfangs', 'zenheadbutt'
];
```

---

## 数据结构

### Pokemon 对象

```javascript
class Pokemon {
    // 基础属性
    name: string;           // 英文名
    cnName: string;         // 中文名
    level: number;          // 等级
    types: string[];        // 属性 ['Fire', 'Flying']
    
    // 种族值
    baseStats: {
        hp: number,
        atk: number,
        def: number,
        spa: number,
        spd: number,
        spe: number
    };
    
    // 个体值
    ivs: { hp, atk, def, spa, spd, spe };
    
    // 努力值
    evs: { hp, atk, def, spa, spd, spe };
    
    // 性格
    nature: string;         // 'Adamant', 'Modest', etc.
    
    // 当前状态
    currHp: number;
    maxHp: number;
    status: string;         // 'par', 'brn', 'slp', 'frz', 'psn', 'tox'
    
    // 能力等级
    boosts: {
        atk: number,        // -6 ~ +6
        def: number,
        spa: number,
        spd: number,
        spe: number,
        accuracy: number,
        evasion: number
    };
    
    // 特性与道具
    ability: string;
    item: string;
    
    // 招式
    moves: Move[];
    
    // 世代机制
    canMega: boolean;
    megaStone: string;
    canDynamax: boolean;
    isDynamaxed: boolean;
    dynamaxTurns: number;
    canTerastallize: boolean;
    teraType: string;
    isTerastallized: boolean;
    
    // 挥发状态
    volatileStatus: {
        confusion: number,
        flinch: boolean,
        substitute: number,
        leechSeed: boolean,
        // ...
    };
}
```

### Move 对象

```javascript
interface Move {
    name: string;           // 英文名
    cn: string;             // 中文名
    type: string;           // 属性
    power: number;          // 威力
    cat: string;            // 'phys', 'spec', 'status'
    accuracy: number;       // 命中率
    pp: number;             // PP
    priority: number;       // 优先级
    
    // 可选字段
    flags?: {
        protect?: 1,        // 可被守住
        contact?: 1,        // 接触类
        sound?: 1,          // 声音类
        punch?: 1,          // 拳类
        bite?: 1,           // 咬类
        // ...
    };
    
    secondary?: {
        chance: number,     // 触发概率
        status?: string,    // 状态异常
        boosts?: object,    // 能力变化
        // ...
    };
}
```

### BattleState 对象

```javascript
interface BattleState {
    // 队伍
    playerParty: Pokemon[];
    enemyParty: Pokemon[];
    playerActive: number;   // 当前出战索引
    enemyActive: number;
    
    // 场地状态
    field: {
        weather: string,    // 'sun', 'rain', 'sandstorm', 'hail'
        weatherTurns: number,
        terrain: string,    // 'electric', 'grassy', 'misty', 'psychic'
        terrainTurns: number,
        trickRoom: number,
        // 场地障碍
        playerSide: {
            stealthRock: boolean,
            spikes: number,
            toxicSpikes: number,
            lightScreen: number,
            reflect: number,
            // ...
        },
        enemySide: { /* 同上 */ }
    };
    
    // 战斗状态
    phase: string;          // 'select', 'battle', 'switch', 'end'
    turn: number;
    locked: boolean;
    
    // 世代机制使用标记
    playerMegaUsed: boolean;
    playerZUsed: boolean;
    playerMaxUsed: boolean;
    playerTeraUsed: boolean;
    
    // 古武系统
    playerStyleCooldown: number;
    enemyStyleCooldown: number;
    
    // 对冲系统
    clashCount: number;
    insightTriggeredThisTurn: boolean;
    insightPredictedMove: Move;
    
    // 训练家信息
    trainer: {
        id: string,
        name: string,
        cnName: string,
        title: string,
        avatar: string
    };
    
    // AI 难度
    aiDifficulty: string;   // 'easy', 'normal', 'hard', 'expert'
}
```

---

## API 参考

### 核心函数

#### `calcDamage(attacker, defender, move, options)`

计算伤害并返回结果。

**参数**：
- `attacker`: Pokemon - 攻击方
- `defender`: Pokemon - 防御方
- `move`: Move - 招式对象
- `options`: Object - 可选参数
  - `isSimulation`: boolean - 是否为模拟计算（不触发副作用）

**返回值**：
```javascript
{
    damage: number,         // 实际伤害
    effectiveness: number,  // 属性克制倍率
    isCrit: boolean,        // 是否暴击
    miss: boolean,          // 是否 Miss
    hitCount: number,       // 命中次数（多段攻击）
    blocked: boolean,       // 是否被守住
    abilityImmune: string,  // 特性免疫名称
    failed: boolean,        // 招式是否失败
    failMessage: string     // 失败原因
}
```

#### `applyDamage(attacker, defender, move, spriteIdRef)`

应用伤害并处理副作用。

**参数**：
- `attacker`: Pokemon
- `defender`: Pokemon
- `move`: Move
- `spriteIdRef`: string - 'player-sprite' 或 'enemy-sprite'

**返回值**：
```javascript
{
    damage: number,
    effectiveness: number,
    isCrit: boolean,
    miss: boolean,
    // ... 同 calcDamage
}
```

#### `getAiAction(aiPoke, playerPoke, difficulty, aiParty, battleContext)`

获取 AI 决策。

**参数**：
- `aiPoke`: Pokemon - AI 当前宝可梦
- `playerPoke`: Pokemon - 玩家当前宝可梦
- `difficulty`: string - 'easy', 'normal', 'hard', 'expert'
- `aiParty`: Pokemon[] - AI 队伍
- `battleContext`: Object - 战斗上下文

**返回值**：
```javascript
{
    type: 'move' | 'switch',
    move?: Move,            // type='move' 时
    index?: number,         // type='switch' 时
    style?: string,         // 'normal', 'agile', 'strong'
    reasoning?: string      // AI 决策理由（调试用）
}
```

#### `executePlayerTurn(player, enemy, move)`

执行玩家回合。

**参数**：
- `player`: Pokemon
- `enemy`: Pokemon
- `move`: Move

**返回值**：
```javascript
Promise<{
    pivot: boolean          // 是否触发换人
}>
```

#### `calculateAnimeGrowth(analysis, result)`

计算动漫风格成长建议。

**参数**：
- `analysis`: Object - 战斗分析
  - `rank`: string - 战斗评级
  - `hpHealth`: number - 剩余 HP 百分比
  - `levelDiff`: number - 等级差
- `result`: string - 'win', 'loss', 'escape', 'caught'

**返回值**：
```javascript
{
    val: number,            // 建议等级提升
    reason: string,         // 成长评估文案
    distribution: string[], // 队伍分配建议
    levelDiff: number,      // 等级差
    avgPlayerLevel: number,
    avgEnemyLevel: number
}
```

### 工具函数

#### `getTypeEffectiveness(attackType, defenderTypes)`

获取属性克制倍率。

**参数**：
- `attackType`: string - 攻击属性
- `defenderTypes`: string[] - 防御方属性数组

**返回值**：number - 克制倍率（0, 0.25, 0.5, 1, 2, 4）

#### `calcStats(pokemon)`

计算宝可梦实际能力值。

**参数**：
- `pokemon`: Pokemon

**返回值**：
```javascript
{
    hp: number,
    atk: number,
    def: number,
    spa: number,
    spd: number,
    spe: number
}
```

#### `getMoveHandler(moveName)`

获取招式处理器。

**参数**：
- `moveName`: string - 招式名称

**返回值**：MoveHandler | null

#### `getMovePriority(move, user, target)`

获取招式优先级。

**参数**：
- `move`: Move
- `user`: Pokemon - 使用者（可选）
- `target`: Pokemon - 目标（可选）

**返回值**：number - 优先级（-7 ~ +5）

---

## 附录

### 常见问题

**Q: 如何添加自定义宝可梦？**

A: 在 `data/pokedex-data.js` 添加数据：
```javascript
'custompokemon': {
    num: 9999,
    name: 'Custom Pokemon',
    types: ['Fire', 'Dragon'],
    baseStats: { hp: 100, atk: 120, def: 90, spa: 110, spd: 80, spe: 100 },
    abilities: { 0: 'Blaze', H: 'Solar Power' },
    // ...
}
```

**Q: 如何调整 AI 难度？**

A: 在战斗初始化时设置：
```javascript
battle.aiDifficulty = 'expert';  // 'easy', 'normal', 'hard', 'expert'
```

**Q: 如何禁用对冲系统？**

A: 设置全局配置：
```javascript
window.GAME_SETTINGS = {
    enableClash: false
};
```

**Q: 如何自定义成长系数？**

A: 修改 `systems/growth-system.js` 中的成长因子权重。

### 性能优化建议

1. **精灵图预加载**：使用 `systems/preloader.js` 预加载常用精灵图
2. **AI 计算缓存**：缓存 AI 评分结果，避免重复计算
3. **减少 DOM 操作**：批量更新 UI，使用 `requestAnimationFrame`
4. **懒加载数据**：按需加载宝可梦数据，而非一次性加载全部

### 调试技巧

**开启调试日志**：
```javascript
window.DEBUG_MODE = true;
```

**查看 AI 决策理由**：
```javascript
const action = getAiAction(aiPoke, playerPoke, 'expert', aiParty, battleContext);
console.log(action.reasoning);
```

**模拟伤害计算**：
```javascript
const result = calcDamage(attacker, defender, move, { isSimulation: true });
console.log(`预计伤害: ${result.damage}`);
```

---

## 更新日志

### v2.0 (2026-01-22)
- ✨ 新增对冲系统（Clash System）
- ✨ 新增古武系统（Move Styles）
- ✨ 新增训练家战术指挥系统
- ✨ 新增杀意感知系统（Insight）
- ✨ 新增动漫风格成长系统
- 🔧 重构模块架构为 ES Modules
- 🔧 优化 AI 引擎（Expert 难度）
- 🐛 修复 Mega 进化形态识别问题
- 🐛 修复极巨化威力计算问题

### v1.5
- ✨ 新增太晶化系统
- ✨ 新增形态变化系统
- 🔧 优化伤害计算精度
- 🐛 修复特性触发时机问题

### v1.0
- 🎉 初始版本发布
- ✨ 完整战斗系统
- ✨ Mega/Z/Dynamax 支持
- ✨ AI 系统（Easy/Normal/Hard）

---

## 致谢

本项目参考并移植了以下开源项目的数据与机制：

- **Pokémon Showdown**: 战斗数据、伤害计算公式
- **PSChina**: 中文翻译数据
- **PokéSprite**: 精灵图资源
- **Nintendo/Game Freak/The Pokémon Company**: 原作版权

---

**文档结束**

如有疑问或建议，请联系开发者或提交 Issue。
